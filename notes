get all info on user
>> select auth_user.id, username, email, user_type from auth_user inner join delivery_user where delivery_user.user_id = auth_user.id;


FIX INDEX IF
FIX PURCHASE WITH ITEM QUANTITY BIGGER THAN STOCK
FIX QUANTITY UPDATE IN CART TO PURCHASE